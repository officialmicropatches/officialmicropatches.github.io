# Ignore the myagency app — moved to its own private repo (micropatches-myagency)
myagency/

# One-time setup script (contains no secrets but doesn't belong in the public repo)
RUN_ME_ONCE.ps1

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
*.swp
